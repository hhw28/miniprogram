<!--components/navi/index.wxml-->

<view class='container'>
  <image bind:tap="onLeft" src="{{latest ? triangleDisLeft : triangleLeft}}" />
  <text>{{title}}</text>
  <image bind:tap="onRight" src="{{first ? triangleDisRight : triangleRight}}" />
</view>

